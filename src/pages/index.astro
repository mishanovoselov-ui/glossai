---
---

<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Glossai — словарь необычных слов</title>
    <meta name="description" content="Glossai — коллекция редких и необычных слов. Расширяй словарный запас, открывай новые смыслы." />
    <meta property="og:title" content="Glossai — словарь необычных слов" />
    <meta property="og:description" content="Glossai — коллекция редких и необычных слов. Расширяй словарный запас, открывай новые смыслы." />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://glossai.art" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Glossai — словарь необычных слов" />
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-CSDJGGSJ7Z"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() { dataLayer.push(arguments); }
      gtag("js", new Date());
      gtag("config", "G-CSDJGGSJ7Z");
    </script>
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <main aria-label="Glossai landing">
      <section class="hero-shell">
        <picture class="hero-media">
          <source media="(max-width: 768px)" srcset="/images/hero-collage-mobile.webp" />
          <img src="/images/hero-collage-desktop.webp" alt="Glossai collage" />
        </picture>
        <header class="header-2" aria-label="Primary">
          <a class="logo" href="/">glossai</a>
          <nav class="nav">
            <a href="#">Concept</a>
            <a href="#">Archive</a>
            <a href="#">About</a>
          </nav>
          <a class="cta" href="#">Open</a>
        </header>
      </section>
    </main>
  </body>
</html>

<style>
  .hero-shell {
    position: relative;
    min-height: 100vh;
    width: 100%;
    overflow: hidden;
  }

  .hero-shell::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(16, 14, 12, 0.38),
      rgba(16, 14, 12, 0.08) 22%,
      transparent 50%
    );
    pointer-events: none;
  }

  .hero-media,
  .hero-media img {
    display: block;
    width: 100%;
    height: 100vh;
  }

  .hero-media img {
    object-fit: cover;
    object-position: center;
  }

  .header-2 {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    width: min(1200px, 100%);
    margin: 0 auto;
    padding: 1.2rem;
  }

  .logo {
    color: #f5eee1;
    text-decoration: none;
    font-size: clamp(1.7rem, 3.4vw, 2.8rem);
    line-height: 1;
    font-weight: 600;
    letter-spacing: -0.03em;
    text-shadow: 0 4px 16px rgba(0, 0, 0, 0.35);
  }

  .nav {
    display: flex;
    align-items: center;
    gap: clamp(0.6rem, 2.6vw, 2rem);
    flex-wrap: wrap;
    justify-content: center;
  }

  .nav a,
  .cta {
    color: #f5eee1;
    text-decoration: none;
    font-family: Space Grotesk, sans-serif;
    font-size: clamp(0.92rem, 1.6vw, 1.1rem);
    line-height: 1.2;
    font-weight: 500;
    text-shadow: 0 4px 16px rgba(0, 0, 0, 0.35);
  }

  .cta {
    border: 1px solid rgba(245, 238, 225, 0.75);
    border-radius: 999px;
    padding: 0.55rem 1rem;
    background: #18141033;
  }

  @media (max-width: 720px) {
    .header-2 {
      padding: 0.85rem 0.8rem;
      flex-direction: column;
      justify-content: center;
    }
    .nav {
      gap: 1rem;
    }
  }
</style>
